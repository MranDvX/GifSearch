import React from "react";
import ReactDOM from "react-dom/client";
import { GiftSearchApp } from "./components/GiftSearchApp";

import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GiftSearchApp />
  </React.StrictMode>
);
